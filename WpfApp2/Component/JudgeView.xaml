<UserControl x:Class="WpfApp2.Component.JudgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp2.Component" xmlns:wpfapp2="clr-namespace:WpfApp2" d:DataContext="{d:DesignInstance Type=wpfapp2:MainViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="电芯检测记录"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Margin="0,6,0,0"
                   Foreground="Gray"
                   Text="表单绑的是 Draft* 属性（类似 Vue 的 v-model）。在「检测记录」里点一行会回填；「保存选中」写回该行。"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,16,0,12">
            <TextBlock Text="条码" VerticalAlignment="Center"/>
            <TextBox Width="140"
                     Margin="8,0,16,0"
                     Padding="4"
                     VerticalContentAlignment="Center"
                     Text="{Binding DraftBarcode, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="电压" VerticalAlignment="Center"/>
            <TextBox Width="80"
                     Margin="8,0,16,0"
                     Padding="4"
                     VerticalContentAlignment="Center"
                     Text="{Binding DraftVoltage, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="新增"
                    Padding="16,6"
                    Command="{Binding AddCommand}"/>
            <Button Content="保存选中"
                    Padding="16,6"
                    Margin="8,0,0,0"
                    Command="{Binding SaveCommand}"/>
            <Button Content="清空表单"
                    Padding="16,6"
                    Margin="8,0,0,0"
                    Command="{Binding ClearCommand}"/>
            <Button Content="删除选中行"
                    Padding="16,6"
                    Margin="8,0,0,0"
                    Command="{Binding DeleteCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
